\documentclass{article}
\begin{document}
\section{Probe}
The first paragraph of the probe runs across several lines of the column and carries a footnote\footnote{A footnote that sits at the foot of the page, below everything else.} in its middle, so that the page holds one below the prose.

A second paragraph stands between the note and the edit, long enough to wrap across a couple of lines of the column.

The paragraph under test is long enough to wrap across a few lines of the column, which gives the rows below an edit something to move when a line grows.

A closing paragraph sits at the foot of the probe and is only here to be pushed down or pulled up.
\end{document}
