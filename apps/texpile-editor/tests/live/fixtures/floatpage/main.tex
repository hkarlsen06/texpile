\documentclass[twoside]{article}
\usepackage{booktabs}
\begin{document}

\section{Opening}

The engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures.

So the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on.

The page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this.

Paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what.

\begin{table}[h]
\centering
\begin{tabular}{lcc}
\toprule
Kind & First & Second \\
\midrule
Alpha & 1 & 2 \\
Beta & 3 & 4 \\
\bottomrule
\end{tabular}
\caption{A table placed here, between two paragraphs.}
\end{table}

A compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the. Before the display
\[
a^2 + b^2 = c^2
\]
after the display the paragraph carries on with a few more ordinary words to finish.

Engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so.

The preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the.

Page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph.

\section{Closing}

From its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own.

Measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the preview can show exactly what a compile would print on the page and nothing else the engine sets every line of this paragraph from its own measures so the.

\end{document}
