\documentclass[11pt]{article}
\begin{document}
\section{Probe}
The first paragraph of the probe runs across several lines of the column, so that the page holds ordinary prose above the lines under test and the edits below have a settled place to land against.

A one line paragraph ends here.

The paragraph after it is long enough to wrap across a few lines of the column, which gives the rows below an edit something to move when a line above them grows by a line or loses one.

A closing paragraph sits at the foot of the probe and is only here to be pushed down or pulled up.
\end{document}
