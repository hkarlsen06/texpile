// the options a panel's segmented control and choice list offer
export type PanelSegment = { value: string; label: string; hint?: string };
export type PanelChoice = { value: string; label: string; detail: string; hint?: string };
